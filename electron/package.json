{
  "name": "Hyperclaw",
  "version": "0.6.0",
  "description": "Hypercho Hyperclaw - AI-powered desktop application",
  "author": "Hypercho",
  "main": "main.js",
  "scripts": {
    "electron:dev": "electron ."
  },
  "dependencies": {
    "electron-updater": "^6.1.7",
    "onnxruntime-node": "^1.24.3",
    "python-shell": "^5.0.0"
  },
  "devDependencies": {
    "cross-env": "^7.0.3",
    "electron": "^39.2.7",
    "electron-builder": "^26.8.1"
  },
  "build": {
    "appId": "com.hypercho.hyperclaw",
    "productName": "Hyperclaw",
    "asar": true,
    "directories": {
      "output": "dist-electron"
    },
    "files": [
      "main.js",
      "preload.js",
      "app-config.json",
      "../public/**/*"
    ],
    "win": {
      "target": "nsis",
      "icon": "../public/win.ico",
      "verifyUpdateCodeSignature": false
    },
    "mac": {
      "target": [
        "zip"
      ],
      "icon": "../public/Logopic.png",
      "category": "public.app-category.productivity",
      "hardenedRuntime": true,
      "gatekeeperAssess": false,
      "identity": "18F65824DF8FEF47D0F84D1EE05DC98D91E3F8EB",
      "cscKeyPassword": "HxC0d3$1gn2026!Z1w",
      "entitlements": "./entitlements.plist",
      "entitlementsInherit": "./entitlements.plist"
    },
    "linux": {
      "target": [
        "tar.gz"
      ],
      "icon": "../public/Logopic.png",
      "category": "Utility",
      "artifactName": "Hyperclaw-Linux-${version}.${ext}"
    },
    "nsis": {
      "oneClick": false,
      "allowToChangeInstallationDirectory": true,
      "allowElevation": true,
      "runAfterFinish": true,
      "artifactName": "Hyperclaw-Setup-Remote-${version}.${ext}",
      "createDesktopShortcut": true,
      "createStartMenuShortcut": true,
      "shortcutName": "Hyperclaw",
      "menuCategory": false,
      "displayLanguageSelector": false,
      "uninstallDisplayName": "Hyperclaw"
    },
    "extraResources": [
      {
        "from": "../.next",
        "to": "../.next",
        "filter": [
          "**/*"
        ]
      }
    ]
  },
  "scripts": {
    "electron:build:win:local": "node scripts/set-config.js local && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --win",
    "electron:build:win:remote": "node scripts/set-config.js remote && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --win",
    "electron:build:win:portable": "node scripts/set-config.js local && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --win --config.win.target=portable",
    "electron:build:mac:local": "node scripts/set-config.js local && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --config.mac.target=zip",
    "electron:build:mac:remote": "node scripts/set-config.js remote && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --config.mac.target=zip --x64 --arm64",
    "electron:build:mac:remote:dmg": "node scripts/set-config.js remote && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --config.mac.target=dmg --x64 --arm64",
    "electron:build:mac:dmg": "node scripts/set-config.js local && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=true electron-builder --mac --config.mac.target=dmg",
    "electron:build:mac:zip": "node scripts/set-config.js local && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --mac --config.mac.target=zip",
    "electron:build:linux:remote": "node scripts/set-config.js remote && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --linux",
    "electron:build:linux:local": "node scripts/set-config.js local && npx cross-env CSC_IDENTITY_AUTO_DISCOVERY=false electron-builder --linux"
  }
}
